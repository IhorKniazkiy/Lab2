#include <stdio.h>//підключення бібліотек

int main() {
    int a = 1, b = 10, c = -1;
    float y = (a + b * c) / (2 * a - b - c) + a / (b + 2.0);//оголошення змінних
    printf("Значення функції y = %.2f", y);//виведення результатів 
    return 0;//кінець програми
}